<% @merchant.distinct_invoices.each do |invoice| %>
<div id="invoice-<%= invoice.id %>">
  <h3>Invoice #: <%= link_to "#{invoice.id}", "/merchants/#{@merchant.id}/invoices/#{invoice.id}" %></h3>
  <p>Status: <%= invoice.status %></p>
</div>
<% end %>
